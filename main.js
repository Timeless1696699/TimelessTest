

function preload(){
   
}

function setup(){
    createCanvas(width, height);
}
function windowResized() {
    resizeCanvas(windowWidth, windowHeight);
  }

function draw(){
    background("blue");
    
}